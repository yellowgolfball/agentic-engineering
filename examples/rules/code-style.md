# Code Style Rules

- Keep files under ~500 lines; split when it improves clarity
- Add comments for non-obvious logic; skip boilerplate doc comments
- Use project terminology consistently across code, config, and docs
- Prefer explicit configuration over implicit defaults
